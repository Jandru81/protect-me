import React from 'react';
import { withRouter } from "react-router-dom";
import './ProtectInfo.scss';

const ProtectInfo = (info) => {

    return (
        "Nilveles de protección"
    )
    
    };


export default withRouter(ProtectInfo);